import ToolPageTemplate from "@/components/ToolPageTemplate";

export default function Page() {
  return (
    <ToolPageTemplate title="Regex Tester" description="Use the Regex Tester.">
      <div className="text-gray-300 text-center mt-8">
        🚧 Coming soon: <strong>Regex Tester</strong>
      </div>
    </ToolPageTemplate>
  );
}